"use strict";
const mermaid_core = require("./mermaid.core-067e081c.js");
const luminance = (color) => {
  const { r, g, b } = mermaid_core.Color$1.parse(color);
  const luminance2 = 0.2126 * mermaid_core._.channel.toLinear(r) + 0.7152 * mermaid_core._.channel.toLinear(g) + 0.0722 * mermaid_core._.channel.toLinear(b);
  return mermaid_core._.lang.round(luminance2);
};
const luminance$1 = luminance;
const isLight = (color) => {
  return luminance$1(color) >= 0.5;
};
const isLight$1 = isLight;
const isDark = (color) => {
  return !isLight$1(color);
};
const isDark$1 = isDark;
exports.isDark = isDark$1;
